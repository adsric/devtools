{
	"devDependencies": {
		"svg-sprite": "^1.3.7"
	},
	"private": true,
	"scripts": {
		"setup": "npm install && mkdir -p svg/src",
		"sprite": "svg-sprite --config config/sprite.json svg/src/*.svg"
	}
}
