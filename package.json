{
  "devDependencies": {
    "autoprefixer": "^6.7.0",
    "browser-sync": "^2.18.6",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-cssnano": "^2.1.2",
    "gulp-imagemin": "^3.1.1",
    "gulp-postcss": "^6.3.0",
    "gulp-rename": "^1.2.2",
    "gulp-size": "^2.1.0",
    "gulp-svg-sprite": "^1.3.6",
    "gulp-uglify": "^2.0.0",
    "postcss-import": "^9.1.0",
    "postcss-reporter": "^3.0.0",
  },
  "private": true,
  "scripts": {
    "build": "gulp build",
    "watch": "gulp build:watch",
    "setup": "npm install && mkdir -p javascripts/src && mkdir -p stylesheets/src && mkdir -p svg/src && mkdir -p images/src",
    "start": "npm run build"
  }
}
