{
    "devDependencies": {
        "autoprefixer": "^6.7.7",
        "babel-core": "^6.24.0",
        "babel-preset-es2015": "^6.24.0",
        "browser-sync": "^2.18.8",
        "eslint-config-defaults": "^9.0.0",
        "gulp": "^3.9.1",
        "gulp-babel": "^6.1.2",
        "gulp-concat": "^2.6.1",
        "gulp-cssnano": "^2.1.2",
        "gulp-eslint": "^3.0.1",
        "gulp-imagemin": "^3.1.1",
        "gulp-postcss": "^6.3.0",
        "gulp-size": "^2.1.0",
        "gulp-svg-sprite": "^1.3.6",
        "gulp-uglify": "^2.1.0",
        "postcss-import": "^9.1.0"
    },
    "babel": {
        "presets": [
            "es2015"
        ]
    },
    "eslintConfig": {
        "env": {
            "browser": true
        },
        "extends": "defaults"
    },
    "private": true,
    "scripts": {
        "build": "gulp build",
        "watch": "gulp build:watch",
        "setup": "npm install && mkdir -p javascripts/src && mkdir -p javascripts/vendor && mkdir -p stylesheets/src && mkdir -p svg/src && mkdir -p images/src",
        "start": "npm run build"
    }
}
