{
	"name": "npm-it",
	"description": "npm recipes to automate with cli tools.",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"autoprefixer": "postcss -u autoprefixer -d .tmp src/css/*.css",
		"babel-js": "babel src/js --out-dir .tmp",
		"clean": "rimraf dist/{*.css,*.js,*.map}",
		"cssnano": "postcss -u cssnano -d dist .tmp/*.css && rimraf .tmp",
		"hash": "hashmark -l 12 -r 'dist/*.{js,css}' 'dist/{name}.{hash}{ext}' --silent",
		"icons": "svg-sprite --symbol --symbol-dest=dist --symbol-sprite=sprite.svg --symbol-inline src/icons/*.svg",
		"uglify-js": "uglifyjs .tmp/file1.js .tmp/file2.js -c -m -o dist/bundle.js && rimraf .tmp/*.js",
		"webpack:dev": "NODE_ENV=development webpack",
		"webpack:build": "NODE_ENV=production webpack",
		"setup": "npm install && mkdir -p src/css && mkdir -p src/js && mkdir -p src/svg",
		"serve": "browser-sync start --server --port 8000 --files \"dist/*.css, dist/*.js, **/*.html, !node_modules/**/*.html\"",
		"build:css": "run-s autoprefixer cssnano",
		"build:js": "run-s webpack:build",
		"build": "run-s clean build:* hash",
		"dev": "run-s clean build:css webpack:dev watch",
		"watch:css": "onchange \"src/css\" -- run-s build:css",
		"watch:js": "onchange \"src/js\" -- run-s webpack:dev",
		"watch": "run-p serve watch:*"
	},
	"babel": {
		"presets": [
			[
				"es2015",
				{
					"modules": false
				}
			]
		]
	},
	"browserslist": [
		"> 1%",
		"last 2 versions"
	],
	"devDependencies": {
		"autoprefixer": "^7.1.6",
		"babel-cli": "^6.26.0",
		"babel-core": "^6.26.0",
		"babel-loader": "^7.1.2",
		"babel-preset-es2015": "^6.24.1",
		"babel-preset-stage-0": "^6.24.1",
		"browser-sync": "^2.18.13",
		"cssnano": "^3.10.0",
		"hashmark": "^5.0.0",
		"npm-run-all": "^4.1.2",
		"onchange": "^3.2.1",
		"postcss-cli": "^4.1.1",
		"svg-sprite": "^1.3.7",
		"rimraf": "^2.6.2",
		"uglify-js": "^3.1.10",
		"webpack": "^3.8.1",
		"xo": "^0.18.2"
	}
}
